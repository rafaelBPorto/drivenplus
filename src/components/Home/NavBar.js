import styled from "styled-components"

export default function NavBar(){
    return(
        <ContainerNavBar>
            NavBar
        </ContainerNavBar>
    )
}

const ContainerNavBar = styled.div`
    position:fixed;
    top:0;
    left:0;
`